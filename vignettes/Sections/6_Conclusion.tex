\section{Conclusion} \label{Section:Conclusion}

%Extension of the estimator in \code{stabit} to allow for $n \geq 2$ groups per market for the group formation problem. Currently, it supports $n \geq 2$ groups per market for the roommates problem only.

An extension of the estimator in the \code{stabit2} function is currently in progress. This will allow for two-sided matching data from the college admissions problem. Currently, only one-sided matching data from the group formation and stable roommates problem is supported.
